include ../mixins/search.pug
include ../mixins/auth.pug
include ../mixins/sub-menu.pug

header.header
    nav.navbar
        .container
            .row.align-items-center
                .col-3 
                    .inner-logo 
                        a(href="/")
                            img(src="/images/logo.svg",alt="Logo")
            
                .col-9 
                    .inner-menu-1 
                        ul.nav(style="margin-right: 190px")
                            li(style="font-size: 14px") 
                                a(href="/") Trang chủ
                            li(class="sub-menu",style="font-size: 14px") 
                                - const pathProduct = "/products";
                                a(href="products") Sản phẩm
                                if(layoutCategoryProducts)
                                    +sub-menu(layoutCategoryProducts, pathProduct)
                            li 
                                a(href="/cart") Giỏ hàng (#{miniCart ? miniCart.totalQuantity : "0" })
            
            .row(style="margin-left: -190px;")
                .col-10(style = "top :51px")  
                    +search()
                .col-9(style="margin-left: 350px;margin-bottom :50px;bottom: 55px")
                    ul.nav
                        +auth(user)
                
